<html>
<head>
    <link href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css' rel='stylesheet' type='text/css'>
    <title>Address Book: New Contact</title>
</head>
<body>
    <div class='container'>
        <div class="jumbotron">
            <h1>Address Book</h1>
        </div>
        <h2>You have added a new contact!</h2>
        <h3> {{ new_contact.getLastName }}, {{ new_contact.getFirstName }} {{ new_contact.getMidInit }} </h3>
        <p>
            Address: <br>
            {{ new_contact.getStreet }} <br>
            {{ new_contact.getCity }}, {{ new_contact.getState }} | {{ new_contact.getZip }} <br>
            Mobile: {{ new_contact.Mobile }} <br>
            Email: {{ new_contact.getEmail }}
        </p>
        <form action='/'>
            <button type='submit' class='btn'>See All Contacts</button>
        </form>
    </div>
</body>
</html>
